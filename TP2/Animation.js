class Animation {
     /**
     * @constructor
     * @param {inst} animation instants
     * @param {transfMat} transformations matrix
     */
    constructor(inst, transfMat) {
        super();

        this.inst = inst;
        this.transfMat = transfMat;
    }


}